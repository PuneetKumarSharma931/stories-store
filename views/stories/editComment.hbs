<h3>Edit your Comment on {{story.user.firstName}}'s Story</h3>
<div class="row">
    <form action="/stories/comments/edit/{{story._id}}/{{comment.userId}}" method="POST" class="col s12">
    <input type="hidden" name="_method" value="PUT">
        <div class="row">
            <div class="input-field">
                <h5>Your Comment:</h5>
                <textarea name="body" id="body">{{comment.comment}}</textarea>
            </div>
        </div>

        <div class="row">
            <button class="btn green darken-2" type="submit">Edit Comment</button>
            <a href="/stories/{{story._id}}" class="btn blue">Cancel</a>
        </div>
    </form>
</div>
